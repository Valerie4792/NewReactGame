code . (this is typed in terminal)
git init
git add .
git commit -m 'initial commit'
git push
npm i @chakra-ui/react @emotion/react @emotion/styled framer-motion

website similar to bootstrap is https://chakra-ui.com/